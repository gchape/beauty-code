spring:
  application:
    name: backend
  profiles:
    active: dev
  config:
    import: "configserver:${CONFIG_SERVER_URI:http://localhost:8888}"
  cloud:
    config:
      token: ${VAULT_TOKEN:dev-root-token}
